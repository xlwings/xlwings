# Table

See [](table_columns.md) for column insertion and deletion. Column positions are one-based, while `table.columns[0]` uses Python indexing.

```{eval-rst}
.. autoclass:: xlwings.main.Table
    :members:
```
