# Comment

A modern threaded comment is separate from a cell [](note.md). Use {meth}`Range.add_comment <xlwings.Range.add_comment>` to create one. On xlwings Lite, read an existing comment with `await cell.get_comment()`.

```{eval-rst}
.. autoclass:: xlwings.main.Comment
    :members:
```
