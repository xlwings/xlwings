# Comments

Worksheet and workbook collections contain top-level threaded comments. On xlwings Lite, use `await sheet.get_comments()` or `await book.get_comments()` to fetch a collection.

```{eval-rst}
.. autoclass:: xlwings.main.Comments
    :members:
```
