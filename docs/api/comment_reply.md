# CommentReply

Replies belong to a [](comment.md). Use `comment.add_reply(text)` to add a reply, and `await comment.get_replies()` in xlwings Lite to enumerate them.

```{eval-rst}
.. autoclass:: xlwings.main.CommentReply
    :members:
```
